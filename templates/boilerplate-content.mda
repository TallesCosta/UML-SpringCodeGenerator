<%  if(c.hasStereotype("entity")) { %>
<!DOCTYPE html>
<html>
<head>
	<title>${c.name} Boilerplate!</title>
	${boilerplate.setup(c, boilerplate.SEMANTIC_UI, 1)}
</head>
<body>
${boilerplate.startBody()}
${boilerplate.header()}
<!-- FORM EXAMPLE -->
${boilerplate.form()}

<!-- DATATABLE TABLE HTML EXAMPLE -->
${boilerplate.tableHtml()}

<!-- DATATABLE TABLE JS EXAMPLE -->
${boilerplate.tableJs()}
${boilerplate.endBody()}
</body>
</html>
<% } %>